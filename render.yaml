services:
  - type: web
    name: ai-driven-customer-analytics
    env: python
    plan: free
    buildCommand: python -m pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.enableCORS false
